apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jandi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "jandi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jandi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
