# FastAPI 서버 및 비동기 환경을 위한 기본 패키지
fastapi==0.110.1      # FastAPI 프레임워크 (최신 버전 사용)
uvicorn[standard]==0.29.0 # FastAPI 서버 구동 (ASGI 서버)

# RabbitMQ 메시지 소비를 위한 패키지
pika==1.3.2           # RabbitMQ 클라이언트 (동기/Blocking)

# PostgreSQL 동기 DB 접근 라이브러리 추가
psycopg2-binary==2.9.9 
